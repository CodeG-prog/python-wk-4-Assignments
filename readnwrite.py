def read_and_write(input_filename, output_filename):
    try:
        # Open the input file in read mode
      with open('input_filename', 'r') as infile:
        # Read the content from the input file
        content = infile.read()
        # Modify content (example: Converting to Upper case)
        

    